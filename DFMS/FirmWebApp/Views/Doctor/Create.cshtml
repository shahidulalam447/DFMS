@model DoctorServiceViewModel
@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout_Admin.cshtml";
}

<section class="content">
    <div class="container-fluid">
        <div class="">
            <div class="card-header">
                <h5 class="card-title">Doctor</h5>

            </div>

            <div class="card-body">
                <form asp-action="Create" asp-controller="Doctor" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    @if (!string.IsNullOrEmpty(ViewData["ErrorMessage"] as string))
                    {
                        <div class="alert alert-warning">
                            @ViewData["ErrorMessage"]
                        </div>
                    }
                    <div class="row mx-2">


                        <div class="col-md-6 col-sm-12 col-lg-6">
                            <div class="form-group">
                                <label asp-for="DoctorName" class="control-label">Doctor Name</label>
                                <input asp-for="DoctorName" required class="form-control" placeholder="Enter Doctor Name">
                            </div>
                        </div>
                        <div class="col-md-6 col-sm-12 col-lg-6">
                            <div class="form-group">
                                <label asp-for="DoctorPhone" class="control-label"> Doctor Phone </label>
                                <input asp-for="DoctorPhone" required class="form-control" id="origin" placeholder="Enter Doctor Phone ">
                            </div>
                        </div>
                        <div class="col-md-12 col-sm-12 col-lg-12">
                            <div class="form-group">
                                <label asp-for="DoctorAddress" class="control-label"> Doctor Address </label>
                                <input asp-for="DoctorAddress" class="form-control" id="origin" placeholder="Enter Doctor Address ">
                            </div>
                        </div>
                     </div>
                    <div class="row mx-2">
                        <div class="form-group col-sm-12">
                            <a asp-action="Index" asp-controller="Doctor" class="btn btn-light mr-2">
                                Cancel
                            </a>
                            <button type="submit" class="btn btn-primary">Submit</button>
                        </div>
                    </div>


                </form>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        $(document).ready(function () {
            $('#Health').addClass('menu-open');
            $('#NewDoctor').addClass('active');

        });
    </script>
    <script>
        window.setTimeout(function () {
            $(".alert").fadeTo(500,).slideUp(500, function () {
                $(this).remove();
            });
        }, 4000);
    </script>
  }

